// defines current state of user regarding to Firebase authentication
enum ApplicationAuthenticationState {
  loggedOut,
  signIn,
  signUp,
  loggedIn,
}

enum ApplicationLoggedInState{
  homePage,
  addTask,
}